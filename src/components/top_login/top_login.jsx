import React from 'react';
import PropTypes from 'prop-types';
import styles from './top_login.module.css';

const TopLogin = () => (
  <div className={styles.TopLogin}>
    SICAP
  </div>
);

TopLogin.propTypes = {};

TopLogin.defaultProps = {};

export default TopLogin;
